# 台塑群組網詞彙

- **側欄未讀群組**：聊天側欄中未讀徽章大於零的群組；監控程式可觀察它，但不可因處理它而切換聊天畫面。
- **最後一則訊息**：某個側欄未讀群組在伺服器端最新的一筆訊息。它必須包含完整、未以省略號截斷的內容；側欄預覽不是最後一則訊息的可靠內容來源。
- **補抓**：使用目前已登入的瀏覽器工作階段向聊天服務請求最後一則訊息，不點擊或切換任何群組 UI。
- **側欄預覽**：側欄展示的摘要文字，可作為群組被更新的訊號，但不能作為完整訊息內容。
- **版次**：已部署監控程式的唯一人工遞增識別，格式為 `vYYYY.MM.DD.N`。它用來確認執行端是否正在使用預期程式版本，與固定的程式檔名無關。
- **歷史版本**：執行目錄內同一監控程式之前的 Python 檔案。啟動最新版前，除最新有效版次外的所有同名前綴檔案都移入 `History/`；既有歷史檔不可覆蓋。
- **私有遠端倉庫**：名為 `fpc-chat-monitor` 的 GitHub 非公開同步來源，只授權指定協作者存取；不包含憑證、聊天內容或執行期日誌。
- **手動一鍵同步**：維護者明確啟動的版本保存動作，依序檢查、提交並推送已核准的程式碼；一般程式啟動不會觸發它。
- **執行期狀態檔**：`fpc_state.json`，監控程式在本機保存的暫態處理狀態；不可納入遠端版本庫。
- **線上對照驗證**：以可登入的實際帳號及實際群組，比較未點入與點入群組時取得的最後一則訊息；未點入情境是主要驗收對象。
- **自然訊息驗證**：不為測試發送任何群組訊息，僅以群組正常產生的新訊息作為線上對照驗證的觸發條件。
- **本機驗證設定**：只存在於本機、供線上對照驗證登入實際帳號使用的 `config.json`；不得同步至 GitHub 或遠端主機。
- **嚴格被動擷取**：不點入、不選取、不切換任何聊天群組 UI 的擷取模式；若服務端未主動回傳完整訊息，程式直接轉發側欄預覽。
